# Copy to .env on the VPS and fill in real values.
# Do not commit the real .env — it is gitignored.

BASE_DOMAIN=change-me.sslip.io

POSTGRES_PASSWORD=change-me

JWT_SECRET=change-me
ACCESS_TOKEN_MINUTES=60

PROM_BASIC_AUTH_USER=admin
PROM_BASIC_AUTH_HASH=change-me
